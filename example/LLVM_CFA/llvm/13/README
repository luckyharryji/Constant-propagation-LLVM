A pass in this directory replaces invocations to "sqrt" with approximate code.
Specifically, it removes calls to "sqrt" and it injects new code to compute an approximation of the square root function.
